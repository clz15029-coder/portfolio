# 進捗管理(1週間プラン)

元の詳細プランは `/Users/miyu/.claude/plans/inherited-exploring-wirth.md` を参照。ここでは日ごとの進捗をチェックする。

**運用ルール**: 何か作業が完了するたびに、このファイルのチェックボックスと「メモ」を更新する。

## Day1: Git移行・TOPページ再構築 ✅完了
- [x] リポジトリを `~/dev/portfolio` にクローン、`css/` `js/` 構成へ移行
- [x] `.nojekyll` 追加、初回コミット・push
- [x] GitHub Pages公開設定(リポジトリをPublicに変更した上で有効化)
- [x] 空デプロイ確認(`https://clz15029-coder.github.io/portfolio/`)
- [x] TOPページを複数ページ構成へ再構築、共通ヘッダー/フッター確立
- [x] ABOUT/WORKS/PROCESS・SKILLS/PERSONAL WORKS/CONTACTの雛形(準備中ページ)を作成
- [x] 配色をモノトーン寄りに変更、ナビ現在地ハイライト追加
- [x] レイアウトを左サイドバーナビ方式に変更(参考サイトのスクリーンショットを反映)
- [x] TOPページに角丸ヒーローカード、WORKSページにフィルタピル+カードグリッドの見た目を追加

メモ: 2026-08-19、ユーザー提供のmorohoshi.siteスクリーンショットを受けて、上部ナビ→左サイドバーに構造変更。DESIGN.mdに参考ポイントを追記済み。

メモ: 当初モノトーン配色に見出し明朝体(Shippori Mincho)を採用していたが、参考サイト(morohoshi.site)がゴシック統一だったため、Zen Kaku Gothic Newに変更(2026-08-19)。

## Day2: WORKS一覧 + データ管理 ✅完了
- [x] `js/works-data.js` 作成(実データ6件、PDFの制作実績より)
- [x] `works.html` グリッド表示(実データ連動)
- [x] カテゴリフィルタ実装(動的生成、WEB/ロゴ/印刷物)

## Day3: 作品詳細テンプレート ✅完了(Day2と合わせて前倒し)
- [x] `work-detail.html` + `js/work-detail.js`
- [x] 制作プロセス表示(実データ:各案件のブラッシュアッププロセス)
- [x] 一覧カードから詳細への導線(`?id=`)

メモ: 2026-08-19、ユーザーから実際のポートフォリオPDF(杉山弥優＿制作実績＿最終版)を受領。氏名を実名(杉山弥優)に置き換え、TOPページのキャッチコピーをPDF記載の実際のタグラインに変更。WORKS6件は前職(AIコミュニケーション)での実案件(タクシーチケット/横浜ウィークリー/柴田設備/三田工業所/日化産業/横浜ケーブルビジョン)の実際の概要・プロセスを使用。作品画像は未挿入(プレースホルダーのまま)。ABOUT/PROCESS・SKILLS/PERSONAL WORKSの実データ化はDay4〜5で対応予定(PDFにプロフィール・スキル・卒業制作・イラスト制作の内容あり)。
また、MV下のテキストが中央揃え/左揃えで混在していた不具合(`.hero-text p`の`margin:0 auto`)を修正。

## Day4: ABOUT + PROCESS・SKILLS ✅完了
- [x] ABOUTページ本実装(実プロフィール・経歴・心がけていること・Instagram)
- [x] PROCESS・SKILLSページ本実装(実スキル5項目・対応業界・ツール・仕事への向き合い方)

## Day5: PERSONAL WORKS + CONTACT ✅完了
- [x] PERSONAL WORKSページ本実装(卒業制作wan-idea/JIDA賞、イラスト制作)
- [x] CONTACTページ本実装(メール+Instagram)

メモ: 2026-08-19、Day4/5もPDFの実データで前倒し完了。サイドバーのSNSリンクは実在するInstagramのみに統一(架空のX/Twitterリンクを削除)。MVキャプションのfont-style:italicがZen Kaku Gothic Newに合成表示され「がたつく」不具合も修正(letter-spacingに変更)。残るのは、作品画像・プロフィール写真などの実素材の挿入のみ(Day6〜7、または素材準備後に別途対応)。

メモ: 2026-08-19、ユーザーの意向でindex.html/about.htmlの「名古屋工業大学にて建築・プロダクト・デジタルプロダクトを横断的に学び」という大学時代の自己紹介文を削除。また、PERSONAL WORKSページ(卒業制作wan-idea/イラスト制作)の内容を一旦全部空にした(ページヘッダーのみ残存、作品セクションは削除)。上記Day5の「PERSONAL WORKSページ本実装」は現状クリアされた状態。

メモ: 2026-08-19、ルート直下が煩雑になってきたためファイル整理を実施。制作開始前の初期プラン`portfolio-plan.md`(Jekyll想定など現行実装と食い違う内容)を`docs/archive/`へ移動しアーカイブ扱いに。ほぼ空だった`README.md`を、フォルダ構成・各ドキュメントの役割・ローカル確認方法をまとめた内容に整備。

メモ: 2026-08-19、さらにユーザーの意向で、サイト本体(html/css/js/images)とメモ類を明確に分離。`DESIGN.md`/`PLAN.md`/`作品の追加方法.md`/`docs/archive/`を`memo/`フォルダにまとめて移動(`docs/`は廃止)。`CLAUDE.md`は自動読み込みの都合上、`README.md`はGitHubの慣習上、それぞれルート直下のまま維持。CLAUDE.mdの参照パスも`memo/PLAN.md`・`memo/DESIGN.md`に更新。

## Day6: 横断ポリッシュ ✅完了
- [x] 全ページ・全内部リンクの棚卸し(7ページ×6リンク、すべて相対パスで正常)
- [x] 未使用CSS(`.container`)を削除
- [x] 本文が長いセクション(ABOUT概要・スキル説明)に最大幅を追加し、横長画面での可読性を改善
- [x] `.meta-grid`を固定3列→auto-fitに変更(ABOUTの4項目で余りが出る問題を解消)
- [x] JSファイルの構文チェック(括弧・テンプレートリテラルの対応確認)

メモ: 2026-08-19、ABOUTページの「和して同ぜず、誠を尽くす」の一言はユーザーの意向で削除(実感と合わないとのこと)。

メモ: 2026-08-19、PERSONAL WORKSページ(`personal-works.html`)に、作品を追加する際にコピペで使えるテンプレートをHTMLコメントとして追加。「深掘り型」(説明文+使用ソフト+外部リンク)と「ライト型」(画像+タイトル+使用ソフトのみ)の2種類。CSSに`.work-meta`(使用ソフトなどの補足行)を追加。`memo/作品の追加方法.md`にPERSONAL WORKS用のセクションを追記。

## Day7: 最終QA・公開確認 ✅完了
- [x] 全ページ・全リンクのクリック確認
- [x] 本番URLでの相対パス確認
- [x] 「作品を追加する方法」メモ作成

メモ: 2026-08-19、本番URL(https://clz15029-coder.github.io/portfolio/)に対して全7ページ・全CSS/JSアセットのHTTPステータスを確認、すべて200。相対パスも`/portfolio/`のサブディレクトリ配下で問題なく解決。大学時代の自己紹介文の削除・PERSONAL WORKS空化も本番に反映済みであることを確認。

メモ: 2026-08-19、ユーザー提供のPDFポートフォリオ(杉山弥優＿制作実績＿最終版)を参照し、PERSONAL WORKSページに卒業制作(wan-idea、深掘り型)とイラスト制作(ライト型、使用ソフト：CLIP STUDIO PAINT / Adobe Photoshop CC)を復元。ライト型は`.light-works-grid`で横並び最大3つまでのグリッド表示に変更(CSS追加)。また、WORKS一覧ページのカードにも制作期間(`period`)をカテゴリタグの右側に表示するよう`js/works-list.js`とCSSを更新。

メモ: 2026-08-19、サイドバーナビの並び順をユーザーの意向で「Works→Personal Works→Process→About→Contact」に変更(全7ページ共通、`js/main.js`の現在地ハイライトは順不同ロジックのため影響なし)。あわせて「Process / Skills」ページから「Process」ページに変更し、スキル・対応業界・ツールのセクションをABOUTページへ移動(概要の直後、心がけていることの前)。process-skills.htmlは制作アプローチ・仕事への向き合い方のみのProcessページとして残存(ファイル名・URLは変更していない)。

メモ: 2026-08-19、ユーザーの意向で卒業制作(wan-idea)とイラスト制作をPERSONAL WORKSページから再度削除。代わりに、PDFポートフォリオ31〜34ページ相当のライト型21件(コーポレート5件・サービスサイト3件・LP/コンペ5件・ロゴ6件)を仮データとして`.light-works-grid`に追加。各カードの期間欄は実データが無いため「期間：XXXX(要変更)」というプレースホルダーにし、後で差し替えが必要なことが分かるようにした。ページ冒頭に仮データである旨の注記(`.coming-soon`パターン再利用)も追加。

メモ: 2026-08-19、ユーザーの意向でProcessページの内容もABOUTページへ統合。process-skills.htmlは削除し、サイドバーナビは「Works→Personal Works→About→Contact」の4項目に(全ページ共通)。ABOUTページの構成は 概要→デザインプロセス(旧Processの導入文)→スキル→対応してきた業界→ツール→仕事への向き合い方(旧Processのカード)→心がけていること→Instagram、の順。README.mdのフォルダ構成表からもprocess-skills.htmlの行を削除。

メモ: 2026-08-19、WORKS周りを大きく再構成。(1) 作品カードをクリックした時、ページ遷移せず画面右から半分幅(58vw/最大640px)のモーダルパネルで詳細を表示する「半分モーダル」形式に変更(`js/main.js`にモーダル開閉ロジック、`js/works-list.js`にクリックハンドラ・pushState/popstate制御を追加)。URLは`work-detail.html?id=`にpushStateで書き換わるため、直接リンク・共有・リロードはフルページ版(`work-detail.html`)が引き続き機能する。(2) 作品詳細のレイアウトをユーザー提供のリファレンス画像に合わせ、タイトル→タグ(カテゴリ+「Clientwork」固定タグ)→説明文→Client/Scope/Yearのリスト→区切り線→URLリンク、というコンパクトな構成に統一。旧来の「概要(箇条書き)」見出しと「制作プロセス」セクションは表示しないことにした(データの`process`配列自体は残置、将来復活可能)。共通レンダリングは`js/work-render.js`の`renderWorkDetailHTML()`に集約し、フルページ版・モーダルの両方で使用。(3) PERSONAL WORKSページ(`personal-works.html`)を廃止してWORKSに統合。`works-data.js`にカテゴリ`personal`(個人制作)・`illustration`(イラスト)を追加し、卒業制作(wan-idea)とイラスト制作をこの2カテゴリの作品として再登録(id:07,08)。PDFポートフォリオ31〜34ページの仮データ21件(id:09〜29)も、ジャンルに応じて既存カテゴリ(web/banner/logo)に振り分けてworks-dataに統合。`client`フィールドを全作品に追加(既存6件は実データ、新規21件は`XXXX(要変更)`のプレースホルダー)。サイドバーナビは「Works→About→Contact」の3項目に簡素化。

メモ: 2026-08-19、ユーザーの意向でWORKSのカテゴリ体系を「できること・形としてイメージできるもの」軸の大分類3つに再編。デジタル・Web系(Webサイト/LP、デジタル広告・SNS用素材)、グラフィック・印刷(フリーペーパー・パンフレット、ビジネスツール、イラストレーション)、ブランディング・CI/VI(ロゴ・シンボルマーク、ガイドライン)の3大分類7小分類を`js/category-groups.js`(新規)に定義。WORKS一覧のフィルタピルも「大分類ラベル→その下に小分類ピル」の二階層UIに変更(`js/works-list.js`)。既存28件のcategoryを新しいkeyに振り分け直した。「卒業制作 - wan-idea」(プロダクトデザイン)はどの分類にも当てはまらないため、ユーザーの意向でWORKS一覧から除外(works-dataからも削除。詳細は過去コミット履歴に残る)。

メモ: 2026-08-19、フィルタピルの大分類ラベル(「デジタル・Web系」等)はユーザーの意向で表示から削除、全ピルを一列に並べるフラット表示に戻した(分類データ自体は`js/category-groups.js`に残置)。また、about.htmlのプロフィール内容(MBTI削除、職歴更新、ツール欄・使用言語の追加など)をユーザーの編集分としてそのままコミット。

メモ: 2026-08-19、3件のリクエストに対応。(1) FAQページ(`faq.html`)を新設、サイドバーナビはCONTACTの直前に配置(内容は「準備中」のプレースホルダーのみ、質問追加用のコメントテンプレート付き)。(2) `works-data.js`の内容を`memo/works-content.csv`としてCSV出力(列: id/category/title/client/period/role/url/summary/point1-3、tagsはcategoryから自動生成のため含めず)。ユーザーがExcel等で編集後、再度Claude Codeに渡して`works-data.js`へ流し込む運用を想定し、手順を`memo/作品の追加方法.md`に追記。(3) TOPページのWorksセクションを、静的な3カテゴリ紹介カードから、大分類3つ(デジタル・Web系/グラフィック・印刷/ブランディング・CI/VI)ごとに実データから最大3件をランダム抽出して表示する動的な仕組みに変更(`js/top-works.js`新規、カード生成は`js/work-render.js`の`renderWorkCardHTML()`をworks-list.jsと共用)。グラフィック・印刷は現状2件しかないため、その2件がそのまま表示される。

メモ: 2026-08-19、ユーザーから提供されたFAQ本文(Q1〜Q7、制作の進め方・修正対応・効率化などに関する質問と回答)をfaq.htmlに反映(暫定版と明言あり)。また、TOPページのContactセクションの説明文を、contact.htmlの導入文(生成AI活用のデザインシステム構築・社内ツール開発支援についての案内)と同じ内容に変更。

メモ: 2026-08-20、FAQページをアコーディオン形式(質問をクリックすると回答が開閉)に変更。ユーザーからmorohoshi.site(参考サイト)のNewsセクションのような見た目を指定されたが、このセッションの環境にはブラウザが無く実際の見た目を確認できなかったため、既存のモノトーン角丸デザインに合わせた標準的なアコーディオン(角丸16pxのカード、＋アイコンが開閉で−に変化、grid-template-rowsによる開閉アニメーション)として実装(`js/faq.js`新規、CSSはstyles.cssに追記)。見た目の差異があれば要調整。

メモ: 2026-08-20、ABOUTページの見出し直下に`.profile-photo`(160×160px角丸プレースホルダー)を追加、プロフィール画像の挿入先を用意。また、FAQの質問行ホバー時に背景が変わる範囲がおかしく見えるという指摘を受け、`.faq-list`の幅が他の本文(720px)より広く`.content`いっぱいに広がっていたことが原因と判断し、`max-width: 720px`を追加して他セクションと揃えた(あわせて`.faq-question`にborder-radius: inheritも追加)。なお、about.htmlの生年月日欄がユーザーの編集で`<span id="age"></span>`(年齢を動的計算する想定と思われる)に変わっているが、対応するJSはまだ無く、現状は空欄表示になる点を本人に共有済み。

メモ: 2026-08-20、ユーザーから4件の改善依頼(Notion CMS連携は「CSV運用のままでOK」と取り下げ、残り3件を実施)。(1) モバイル時のハンバーガーメニュー(サイドバー)を、左からではなく右からスライドインするよう変更(`css/styles.css`の`@media (max-width:880px)`内、`.sidebar`のleft→right・translateXの符号を反転、`.detail-modal`の左方向シャドウに合わせて`.sidebar.is-open`のbox-shadowも調整)。(2) TOPページMV(`.hero-visual`)に、`worksData`からランダムに1件選んで3〜5秒間隔で切り替え表示する仕組みを追加(`js/hero-visual.js`新規)。`works-data.js`のスキーマは変更せず、`image`フィールドが無い作品はタイトルのテキスト表示にフォールバックする設計(将来`image`を追記すれば自動的に画像表示に切り替わる。`memo/作品の追加方法.md`に追記済み)。(3) NEWSページ(`news.html`)を新規作成、サイドバーナビはFAQとContactの間に配置(全ページ共通)。日付+見出し+本文の`.news-item`をシンプルに並べる形式で、最初の1件(サイト更新のお知らせ)を実データとして掲載、追加用のコメントテンプレートも用意。

メモ: 2026-08-20、上記(3)のNEWSページをFAQと同じ`.faq-item`アコーディオン部品に作り直し(ユーザーの意向で「NewsもFAQと同じパーツを使いたい」)。日付+見出しをクリックすると本文が開く形式に変更、`js/faq.js`(クラス名ベースの汎用実装)をそのまま読み込んで動作させている。専用だった`.news-list`/`.news-item`のCSSは削除し、日付表示用の`.news-date`のみ残した。あわせて、FAQ/NEWS共通のアコーディオン(`.faq-answer`)が閉じている間、`grid-template-rows`による高さ0だけでなく`visibility: hidden`も付与するよう修正(「閉じているときに見えないようにしてほしい」という指摘への対応。開閉アニメーションが終わるまでは`visibility`の切り替えをtransition-delayで遅らせている)。

メモ: 2026-08-20、上記の`visibility: hidden`対応後も「faq-answerが非表示になっていない」という指摘が再度あり(スクリーンショット2枚を提示、うち1枚は開いたQ1に青い枠線が付いていた)。`grid-template-rows`+`visibility`の組み合わせはブラウザによって完全に高さ0にならない可能性があると判断し、確実性を優先して`.faq-answer { display: none; }` / `.faq-item.is-open .faq-answer { display: block; }` というシンプルな方式に変更(開閉アニメーションは無くなったが、閉じている時は完全にレイアウトから外れるため誤表示の可能性が無くなる)。

メモ: 2026-08-31、これまで未対応だった「WORKS一覧・詳細ページへの作品画像の実挿入」の仕組みを実装。`works-data.js`の各作品に`images`(配列、複数枚対応)フィールドを追加し、1枚目が一覧カード・MVランダム表示のサムネイル、2枚以上入れると作品詳細(モーダル/フルページ共通)にギャラリー表示される設計にした(`js/work-render.js`の`renderWorkCardHTML()`/`renderWorkDetailHTML()`、CSSに`.work-gallery`を追加)。あわせて、旧`image`(単数)フィールドを使っていた`js/hero-visual.js`も新しい`images`配列を見るように統一。現時点ではまだ実画像ファイル自体は未配置(`images: []`のまま)なので、引き続き「作品画像(準備中)」表示。手順は`memo/作品の追加方法.md`に追記済み。

メモ: 2026-08-31、ユーザーが`images/works/`に配置した12枚のサムネイル画像(いずれも1200×900px=4:3)を実際に反映。元は日本語クライアント名のPNG(834KB〜1MB前後のものも含む)だったため、`sips`でJPEG(quality 82)に変換して`01.jpg`〜`12.jpg`(作品IDベース)にリネーム、全ファイル300KB以下に軽量化した。11枚は既存作品(id: 01〜11)に対応、残り1枚(`株式会社久栄_サイトようイラスト作成.png`)はworks-data.jsに存在しない新規作品だったため、id: '12'として追加(`category: 'illustration'`、`period`/`role`/`summary`は詳細不明のため`'XXXX(要変更)'`のプレースホルダー、既存id 09〜29と同じ運用)。全12作品の`images`配列に`images/works/{id}.jpg`を設定済み。id 12の実際の制作期間・担当範囲・説明文は別途ヒアリングして差し替えが必要。

## スコープ外(今週やらないこと)
- 実案件6〜10件の実データ、実写真、実プロセス資料
- Notion手順化の実スクリーンショット、ABOUT本文・実写真
- 画像最適化、誤字チェック、独自ドメイン設定
